using System.Web;
using QFramework;
using SimpleHttpClient;
using UnityEngine;

namespace LaunchPage
{
    public class LaunchPage : Architecture<LaunchPage>
    {
        protected override void Init()
        {
            // RegisterUtility<HttpUitls>(new HttpUitls());
        }
    }
}
